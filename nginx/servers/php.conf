
server {
    listen       80;
    server_name  wrz.php.com;

    #charset koi8-r;


    client_max_body_size 1024m;
    fastcgi_connect_timeout 300;
    fastcgi_send_timeout 600;
    fastcgi_read_timeout 600;
    fastcgi_temp_file_write_size 256k;

    access_log  /Users/wrz/Project/php-project/12xuecms.log;

    root   /Users/wrz/Project/php-project/wwwroot;



    error_log /Users/wrz/Project/php-project/12xuen_errorcms.log info;


    location / {
        index  index.html index.htm index.php;
            if (!-e $request_filename)
        {
        rewrite ^(.*)$ /index.php?s=$1 last;
        break;
            }
   }

        #error_page  404              /404.html;

        # redirect server error pages to the static page /50x.html
        #
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }

        # proxy the PHP scripts to Apache listening on 127.0.0.1:80
        #
        #location ~ \.php$ {
        #    proxy_pass   http://127.0.0.1;
        #}

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        #
        location ~ \.php {
            fastcgi_pass   127.0.0.1:9000;
            fastcgi_index  index.php;
            fastcgi_split_path_info ^(.+\.php)(.*)$;     #增加这一句
            fastcgi_param PATH_INFO $fastcgi_path_info;    #增加这一句

            fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include        fastcgi_params;
            fastcgi_buffer_size 128k;
            fastcgi_buffers 32 32k;
            fastcgi_busy_buffers_size 256k;
        }

        # deny access to .htaccess files, if Apache's document root
        # concurs with nginx's one
        #
        #location ~ /\.ht {
        #    deny  all;
        #}
    }


server {
        listen       80;
        server_name dev.12xue.com;

        #charset koi8-r;

        client_max_body_size 1024m;
        fastcgi_connect_timeout 300;
        fastcgi_send_timeout 600;
        fastcgi_read_timeout 600;
        fastcgi_temp_file_write_size 256k;

        root   /Users/wrz/dev/cms.12xuetest.com/wwwroot;

        access_log  /Users/wrz/logs/php/12xuecms.log;
        error_log /Users/wrz/logs/php/12xuen_errorcms.log info;

        set $cors_origin "";
        location / {
            if ($http_origin ~* ".*12xuetest.com") {
                set $cors_origin $http_origin;
            }
            if ($http_origin ~* ".*12xuecms.com") {
                set $cors_origin $http_origin;
            }
            if ($http_origin ~* ".*12xue.com") {
                set $cors_origin $http_origin;
            }

            add_header 'Access-Control-Allow-Origin' $cors_origin;
            add_header Access-Control-Allow-Methods 'GET, POST, OPTIONS';
            add_header Access-Control-Allow-Headers 'DNT,X-Mx-ReqToken,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization';
            index  index.html index.htm index.php;
            if (!-e $request_filename)
            {
                rewrite ^(.*)$ /index.php?s=$1 last;
                break;
            }
   }

        #error_page  404              /404.html;

        # redirect server error pages to the static page /50x.html
        #
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }

        # proxy the PHP scripts to Apache listening on 127.0.0.1:80
        #
        #location ~ \.php$ {
        #    proxy_pass   http://127.0.0.1;
        #}

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        #
        location ~ \.php {
            fastcgi_pass   127.0.0.1:9000;
            fastcgi_index  index.php;
            fastcgi_split_path_info ^(.+\.php)(.*)$;     #增加这一句
            fastcgi_param PATH_INFO $fastcgi_path_info;    #增加这一句

            fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include        fastcgi_params;
            fastcgi_buffer_size 128k;
            fastcgi_buffers 32 32k;
            fastcgi_busy_buffers_size 256k;
        }

        # deny access to .htaccess files, if Apache's document root
        # concurs with nginx's one
        #
        #location ~ /\.ht {
        #    deny  all;
        #}
    }

# 12xue UI
server {
    listen       80;
    server_name devui.12xue.com;

    #charset koi8-r;

    client_max_body_size 1024m;
    fastcgi_connect_timeout 300;
    fastcgi_send_timeout 600;
    fastcgi_read_timeout 600;
    fastcgi_temp_file_write_size 256k;

    root   /Users/wrz/dev/12xueNewUI/public/;

    access_log  /Users/wrz/logs/php/12xueui.log;
    error_log /Users/wrz/logs/php/12xuen_errorui.log info;

    set $cors_origin "";
    location / {
        # if ($http_origin ~* ".*12xuetest.com") {
        #     set $cors_origin $http_origin;
        # }
        # if ($http_origin ~* ".*12xuecms.com") {
        #     set $cors_origin $http_origin;
        # }
        # if ($http_origin ~* ".*12xue.com") {
        #     set $cors_origin $http_origin;
        # }

        add_header 'Access-Control-Allow-Origin' $cors_origin;
        add_header Access-Control-Allow-Methods 'GET, POST, OPTIONS';
        add_header Access-Control-Allow-Headers 'DNT,X-Mx-ReqToken,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization';
        index  index.html index.htm index.php;
        if (!-e $request_filename)
        {
            rewrite ^(.*)$ /index.php?s=$1 last;
            break;
        }
    }

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   html;
    }

    # proxy the PHP scripts to Apache listening on 127.0.0.1:80
    #
    #location ~ \.php$ {
    #    proxy_pass   http://127.0.0.1;
    #}

    # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
    #
    location ~ \.php {
        fastcgi_pass   127.0.0.1:9000;
        fastcgi_index  index.php;
        fastcgi_split_path_info ^(.+\.php)(.*)$;     #增加这一句
        fastcgi_param PATH_INFO $fastcgi_path_info;    #增加这一句

        fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include        fastcgi_params;
        fastcgi_buffer_size 128k;
        fastcgi_buffers 32 32k;
        fastcgi_busy_buffers_size 256k;
    }
}


server {
    listen       80;
    server_name qdui.12xuetest.com;

    client_max_body_size 1024m;
    fastcgi_connect_timeout 300;
    fastcgi_send_timeout 600;
    fastcgi_read_timeout 600;
    fastcgi_temp_file_write_size 256k;

    root   /Users/wrz/dev/qingdao12xueNewUi/public/;

    access_log  /Users/wrz/logs/php/qingdao12xueui.log;
    error_log /Users/wrz/logs/php/qingdao12xuen_errorui.log info;

    set $cors_origin "";
    location / {
        add_header 'Access-Control-Allow-Origin' $cors_origin;
        add_header Access-Control-Allow-Methods 'GET, POST, OPTIONS';
        add_header Access-Control-Allow-Headers 'DNT,X-Mx-ReqToken,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization';
        index  index.html index.htm index.php;
        if (!-e $request_filename)
        {
            rewrite ^(.*)$ /index.php?s=$1 last;
            break;
        }
    }

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   html;
    }

    # proxy the PHP scripts to Apache listening on 127.0.0.1:80
    #
    #location ~ \.php$ {
    #    proxy_pass   http://127.0.0.1;
    #}

    # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
    #
    location ~ \.php {
        fastcgi_pass   127.0.0.1:9000;
        fastcgi_index  index.php;
        fastcgi_split_path_info ^(.+\.php)(.*)$;     #增加这一句
        fastcgi_param PATH_INFO $fastcgi_path_info;    #增加这一句

        fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include        fastcgi_params;
        fastcgi_buffer_size 128k;
        fastcgi_buffers 32 32k;
        fastcgi_busy_buffers_size 256k;
    }
}